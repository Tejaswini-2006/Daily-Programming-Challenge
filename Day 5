/*
Day 5 of 30 

Problem : Find the Leaders in an Array
You are given an integer array arr of size n. An element is considered a leader if it is greater than all the elements to its right. Your task is to find all such leader elements in the array.

Input :
An integer array arr of size n.
Example : 
arr = [16, 17, 4, 3, 5, 2]

Output :
Return an array containing all the leader elements in the order in which they appear in the original array.
Example:
Leaders: [17, 5, 2]
*/
#include <stdio.h>

int main() {
    int n;
    
    // Step 1: Take input size
    printf("Enter size of array: ");
    scanf("%d", &n);
    
    int arr[n];
    
    // Step 2: Input elements
    printf("Enter %d elements: ", n);
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    
    // Step 3: Create array to store leaders
    int leaders[n];  // max possible leaders = n
    int count = 0;   // number of leaders found
    
    // Step 4: Start from rightmost element
    int max_from_right = arr[n-1];
    leaders[count++] = arr[n-1];  // last element is always a leader
    
    // Step 5: Traverse from right to left
    for(int i = n-2; i >= 0; i--) {
        if(arr[i] > max_from_right) {
            max_from_right = arr[i];
            leaders[count++] = arr[i];
        }
    }
    
    // Step 6: Leaders were stored in reverse order
    printf("Leaders in the array: ");
    for(int i = count-1; i >= 0; i--) {
        printf("%d ", leaders[i]);
    }
    
    return 0;
}
OUTPUT
Enter size of array: 5
Enter 5 elements: 17
5
53
6
4
Leaders in the array: 53 6 4 

