/* Day 22 of 30 

Problem : First Element to Repeat k Times
You are given an array of integers and an integer k. Your task is to find the first element in the array that appears exactly k times. If no such element exists, return -1.

Input :
* An integer array arr of size n, where 1≤n≤105
* An integer k, where 1≤k≤n
* Input: arr = [3, 1, 4, 4, 5, 2, 6, 1, 4], k = 2

Output :
Return the first element from the array that occurs exactly k times. If no element occurs exactly k times, return -1.
Output: 1
*/


#include <stdio.h>
#include <stdlib.h>

// Function to find the first element that occurs exactly k times
int firstElementKTimes(int arr[], int n, int k) {
    int freq[100000] = {0};  // Frequency array (assuming constraints n ≤ 1e5)

    // Count frequencies
    for (int i = 0; i < n; i++) {
        freq[arr[i] + 50000]++;  // Offset added to handle negatives
    }

    // Find first element that occurs exactly k times
    for (int i = 0; i < n; i++) {
        if (freq[arr[i] + 50000] == k) {
            return arr[i];
        }
    }

    return -1; // If no element occurs exactly k times
}

int main() {
    int n, k;
    printf("Enter size of array: ");
    scanf("%d", &n);

    int arr[n];
    printf("Enter array elements: ");
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    printf("Enter k: ");
    scanf("%d", &k);

    int result = firstElementKTimes(arr, n, k);
    printf("Output: %d\n", result);

    return 0;
}
output
n = 9
arr = 3 1 4 4 5 2 6 1 4
k = 2
1

